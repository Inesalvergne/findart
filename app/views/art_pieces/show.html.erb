
<div class="container-show-artpiece">

    <div class="wrapper-show-img">
    <% if @artpiece.cloud_image.present? %>
      <%= cl_image_tag @artpiece.cloud_image.key, crop: :fill %>
    <% else %>
      <%= image_tag "dummy-painting.jpg" %>
    <% end %>
    </div>

    <div class="wrapper-show-description">

      <h2> <%= @artpiece.title %> </h2>
      <p> By <%= @artpiece.artist %> </p>
      <p> <%= @artpiece.description %> </p>
      <p class= "btn btn-info rounded-3"> <%= @artpiece.category %> </p>
      <p class= "btn btn-info rounded-3"><%= @artpiece.style %></p>
      <p id= "price"> Rent it for <span>$<%= @artpiece.price_rate %> </span>a day </p>

      <div class="wrapper-booking-form">
        <hr>
        <h3> Book this artwork</h3>
        <%= simple_form_for [ @artpiece, @offer ] do |f| %>
          <%= f.input :start_date %>
          <%= f.input :end_date %>
          <%= f.submit "Send a request", class: "btn btn-primary" %>
        <% end %>
      </div>

    </div>

</div>


<div class="container-owner-card">

  <h2> About the Owner </h2>

  <div class="container-owner-info">
    <div class= "owner-bio">
      <p> <strong> <%= "#{@artpiece.user.first_name} #{@artpiece.user.last_name}" %> </strong></p>
      <p> <%= "#{@artpiece.user.first_name}" %> is the child of a shipping magnate,
      who was shrouded with scandal from drug overdoses to murder've been collectioning arts since.
      His impressive collection includes pieces by Picasso, Warhol, Yves Klein, and Henry Moore, to name a few and is probably worth millions or even billions.</p>
    </div>

  </div>

</div>

<div class="container-suggestions">

  <h2> Explore More Artworks </h2>

  <div class="container-more-artworks">

    <%= render "shared/cards_overview" %>
  </div>

</div>
